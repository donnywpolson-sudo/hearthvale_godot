#:schema https://developers.openai.com/codex/config-schema.json

# Static startup default; AGENTS.md allows adaptive model switching when available.
model = "gpt-5.6-luna"
model_reasoning_effort = "high"
plan_mode_reasoning_effort = "high"
